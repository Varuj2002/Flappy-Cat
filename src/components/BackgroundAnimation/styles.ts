import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    background: {
        position: 'absolute',
        width: 1200,
        height: '100%',
        bottom: -5,
        transform: [
            {
                translateX: 0,
            },
            {
                translateY: 0,
            },
        ],
    },
});

export default styles;
